# 09 · 軟體架構定案（彙整）

> 本文彙整目前的軟體全貌與本輪定案，作單一參考。硬體/外設/韌性見 [08](08_末端外設_Driver_韌性架構.md)；協定細節見 [02](02_通訊協定.md)；設計決策見 [06](06_設計決策與待確認.md)；schema 見 [../schema/](../schema)。
> 實測基準：深鏈 `1-2-3-7`（3×RP2040 中繼 × 1×STM32 末端）已端到端驗通。

---

## 1. 模組地圖

| 模組 | 職責 | 語言 / 平台 |
|---|---|---|
| **C · 協定庫** | 框架 `\n/\r` 編解碼、位址字串操作、CRC | C++ + Python（雙版，契約） |
| **board · 板級庫** | 每顆 MCU 的傳輸 config（bus UART 腳 / DE-RE / 準位 / LED） | C++（末端）+ MicroPython（中繼器），各一份 |
| **E · schema / 型錄** | descriptor 定義 + 所有設備型錄（大 JSON，單一真相） | JSON |
| **A · 末端** | 讀指撥 + 多外設 driver + 本地 PID/AI + 打包回報 | C++ / STM32（F411…H7） |
| **B · 中繼器** | 輪詢 + cache + 前綴 + hot-swap 設定；純轉發 payload | MicroPython / RP2040 |
| **D · PC 上位機** | 請求排程 + 儀表板 + 規則引擎；可對客戶暴露 MODBUS-TCP | Python |
| **F · 傳輸層** | RS485 ↔ Ethernet(W5500) 可插拔 | Python / RP2040 |

## 2. 協定分層（線上）

```
框架    : byte 串流，\n=未完 · \r=完成（batch 多筆）
位址    : 字串路徑 1-2-3-7-1（節點段=DIP、外設段=人為）；逐跳前綴聚合
動詞    : DESC（要說明書）· REQ（要值）· SET（下指令，需 ACK）
編碼    : DESC=JSON（自描述）· REQ=純值 CSV（descriptor 定順序）· 極限才 binary
完整性  : CRC-16/MODBUS（逐跳重算，驗不過丟棄 → stale）
框架外  : age + 健康 envelope(ok/up/rst) 放框架外 → 中繼器純轉發、不碰 payload
```

- **DESC=JSON、REQ=純值 CSV**：JSON 只描述「這是什麼」（偶爾傳），傳輸只送「現在多少」（每輪）。型別/單位/順序由 descriptor 提供，執行期不重傳。
- 範例：`1-2-3-7|25.1,3.2,1013,1,123*A3F1\r`（node 的 3 外設值 + ok + up + CRC）。
- **編碼對中繼器透明**：中繼器不解析 payload，換 JSON/CSV/binary 都不動它。詳見 [02 §編碼](02_通訊協定.md)。

## 3. 資料模型（位址=where、descriptor=what）

- **Node** = 一顆 MCU = 一個 bus 位址（DIP 定）。
- **Peripheral** = node 內一單元，local id **人為定義**（外設段）。
- **Descriptor（JSON）**：node 身分 + `peripherals[]`（type / binding / driver / calibration / measurements）。
- **型錄（catalog）**：所有已知 descriptor 的大 JSON，**預載進中繼器** → hot-swap 靠它（位址 + 型錄 → 知道該位置該是什麼 → 推設定；校正走 per-position）。

## 4. 指標式資料存取（firmware 原則）

**核心：零複製 + 無 heap，資料一律用指標/引用存取（延續 protocol.h/.cpp 的 char-buffer 原則）。**

- **驅動登錄表 = 函式指標表**：`type → {init, read, status}` 函式指標。加新 driver = **加一列指標**，主邏輯不動（無 switch-case 大表、不重編主碼）。
- **外設值槽 = 指標存取**：driver 背景更新自己的值槽；主迴圈打包時**指向**各槽，**不複製**。
- **框架組裝指標化**：REQ 回覆時，按 descriptor 定義的外設順序**逐一指向值槽**串出純值 CSV，免中間 JSON 物件、免暫存大 buffer。
- **AVR/STM32 安全**：char buffer + 指標，不用 `String`/heap（避免碎片、跨板一致）。
- **MicroPython 端對應**：無裸指標 → 原則＝**引用不複製**（cache 存引用、serve 指向 cache 槽、pass-through 不 rebuild payload）。

> 好處：省 RAM、省複製、無 heap 碎片、換編碼/加外設不動主碼。

## 5. 資料流

```
末端 driver 背景取樣 → 本地值槽（本地 PID/AI 自主跑，與輪詢解耦）
   ↑ REQ  末端指向各值槽 → 串出 batch 純值(+CRC)
中繼器 round-robin 輪詢下層 → CRC 驗 → 前綴自己位址 → 存 cache(引用, hold-last)
   ↑ 上層要才回 cache（框架外附 age）；快取解耦：PC 照自己速率撈，不等 bus
PC 排程請求 → 解碼 → 儀表板 + 規則引擎（條件觸發 SET 下行, 需 ACK）
```

## 6. 語言 / 平台分工（各取所長）

| 角色 | 平台 | 為什麼 | 分類 |
|---|---|---|---|
| **中繼器** | MicroPython / RP2040 | dict/json 優勢**用在型錄/路由/hot-swap 設定**；傳輸只 pass-through | 純輸送 / 高速傳送 / 計算與驅動 |
| **末端** | C++ / STM32（F411 通用 · F7/H7 重運算） | FPU+ADC+馬達+5V 容忍；driver + 本地 PID/AI | 監控 / 慢控 / 快控 / 輸出 / 安全 |
| **PC** | Python | 排程 + 儀表板 + 規則；對客戶可暴露 MODBUS-TCP | — |
| **board 庫** | C++ / MicroPython | 每 MCU 傳輸 config，換末端只改 `BOARD` 一行 | — |

## 7. 韌性（不冗餘，四道防線）

**預防**（fail-safe 收發器 + TVS/ESD + 隔離 + 焊死）→ **偵測**（CRC + 健康 envelope + 看門狗）→ **優雅降級**（cache hold-last + 本地 PID fail-operational + safe_state）→ **快速復原**（熱插拔 + 溫備 + 型錄自動配置）。頂層中繼器留熱備（故障半徑大）；共享纜線單點先標記。詳見 [08 §9](08_末端外設_Driver_韌性架構.md)、[06 §H](06_設計決策與待確認.md)。

## 8. 現況 vs 待實作

| | 項目 |
|---|---|
| ✅ 已跑通 | 深鏈 1-2-3-7（4 層）、逐跳前綴、cache 解耦、健康監測、熱插拔容錯、本機工具鏈 |
| ⏳ 待實作（本輪定案） | **DESC/REQ 分離 + 純值 CSV**、**CRC-16/MODBUS 啟用**、**age 移出 payload**、**board 板級庫**、**driver 登錄表(函式指標)**、**指標式值存取**、**SET/控制協定**、指撥定址實裝 |
| 🐛 現況缺口 | 傳輸仍 JSON（過度簡化，沒發揮 MicroPython）、age 疊 3 層、無 CRC |
